line-length = 100
target-version = "py312"

[lint]
select = [
    "E",   # pycodestyle
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # bugbear
    "SIM", # simplify
    "ANN", # annotations
    "RUF",
]

[lint.per-file-ignores]
"**/test_*.py" = ["ANN001", "ANN201"]
